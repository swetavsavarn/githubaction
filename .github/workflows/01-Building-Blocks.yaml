name: 01-Building Blocks
on:
    - push
    - workflow_dispatch

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say-hello
              run: echo "Hello,world"
    
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: "Success step"
              run: |
                echo "I will success"
            - name: "Say goodbye"
              run: "echo goodbye"
                
            
       